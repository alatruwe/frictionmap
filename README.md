# frictionmap

A CLI tool that parses Claude Code session logs and produces a friction map of a codebase — scoring every file by how much friction Claude Code experienced working with it.

**Status:** under construction. Milestone 1 (CLI + interactive HTML report) in progress.
