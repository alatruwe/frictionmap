# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
htmlcov/
.venv/
venv/
env/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local settings (per-machine permissions, env, hooks)
.claude/

# Project artifacts
report.html

# Cache
.cache/

# Internal docs (kept in Claude Desktop project, not in repo)
*_handoff.md
this-project-started-with-polymorphic-rossum.md

# v2 anchor-hunting dump (raw session text — never commit)
anchor_candidates.md
anchor_sessions/

# v2 validation sample (raw session text + sealed assignments — never commit;
# sample-manifest.csv is committed only after labels are, per pre-reg §4)
labeling-sheet.md
sample-manifest.csv
reserve-list.md
relabel-sheet.md
